/* Requires the Docker Pipeline plugin */
pipeline {
    agent { docker { image 'juanpabloinformatica/hellopython:latest' } }
    stages {
        stage('build') {
            steps {
                sh 'python --version'
            }
        }
    }
}
